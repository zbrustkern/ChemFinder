<%= form_for(@fun_group) do |f| %>
  <% if @fun_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fun_group.errors.count, "error") %> prohibited this fun_group from being saved:</h2>

      <ul>
      <% @fun_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
